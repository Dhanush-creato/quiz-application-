package com.quizapp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * QuizApplicationBackend – Entry point for the Spring Boot application.
 */
@SpringBootApplication
public class QuizApplicationBackend {
    public static void main(String[] args) {
        SpringApplication.run(QuizApplicationBackend.class, args);
    }
}
